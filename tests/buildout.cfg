[buildout]
parts =
    python
    django
eggs =
    django-cms
    coverage
    unittest-xml-reporting
    django
develop =
    ../

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}
scripts =
    coverage


[django]
recipe = djangorecipe
version = 1.2.1
project = testapp
settings = settings
eggs = ${buildout:eggs}