#!/bin/bash

set -e

SCRIPTS=$(dirname "$(realpath "$0")")
ROOT=$(git rev-parse --show-toplevel)

cd "${ROOT}/cms"

echo tx pull --force
"${SCRIPTS}/tx" pull --force
echo Done.
django-admin compilemessages

"${SCRIPTS}/filter-locale-changes"
