'use strict';

// #############################################################################
// User logout

casper.test.begin('User Logout', function (test) {
    casper
        .run(function () {
            test.done();
        });
});
